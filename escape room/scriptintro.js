// scriptintro.js

document.addEventListener('DOMContentLoaded', function() 
{
    const startButton = document.getElementById('startButton');
    
    startButton.addEventListener('click', function() 
    {
        alert("Benvenuto nel gioco! Preparati a risolvere gli enigmi.");
        // Puoi anche aggiungere qui del codice per caricare la pagina successiva
        // o cambiare la storia dinamicamente.
    });
});
